# Roadmap (short)

1. Wire frontend to backend endpoints (fixtures, match pages, live socket) — DONE
2. C++ inference demo (CLI + Node integration) — DEMO implemented; integrate binary path via ERA_INFERENCE_BIN
3. Cricket data feed integration: Adapter added — set CRICKET_API_URL/CRICKET_API_KEY to enable
4. Music & stream directory: Streams page added with example embeds (link-outs only)
5. Create issues/milestones: ISSUES_TODO.md provided — run through and open GitHub issues as desired
